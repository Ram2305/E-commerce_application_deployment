---
- hosts: webserver
  become: true
  become_user: root
  tasks:
   - name: installing packages
     yum:
        name:
           - git
           - docker
   - name: start docker
     service: 
        name: docker
        state: started
        
   - name: clone the repo 
     git:
        repo: 
